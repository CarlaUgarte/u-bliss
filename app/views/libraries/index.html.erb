<h3 class="mt-5 mb-5 text-center">Mi Biblioteca</h3>

<div class="container">
  <div class="row mb-5">
    <% @libraries.each do |library| %>
      <div class="col-12 mb-4">
        <div class="card h-100 shadow">
          <div class="card-body d-flex flex-column">
            <h5 class="card-title fw-bold"><%= library.syllabus.title %></h5>
            <p class="card-text"><%= library.syllabus.description %></p>
            <div class="d-flex justify-content-end mt-auto">
              <%= link_to "Ver más", syllabus_path(library.syllabus), class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
