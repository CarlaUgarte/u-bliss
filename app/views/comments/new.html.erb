<h3>Nuevo Comentario</h3>

<%= form_with model: [ @lecture, @comment ], local: true do |form| %>
  <% if user_signed_in? %>
    <div class="form-group">
      <%= form.label :content, "Escribe tu comentario:" %>
      <%= form.text_area :content, rows: 4, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%= form.submit "Comentar", class: 'btn btn-primary' %>
    </div>
  <% else %>
    <p>Por favor, <%= link_to 'inicia sesión', new_user_session_path %> para comentar.</p>
  <% end %>
<% end %>
